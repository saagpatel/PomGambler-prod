# DOC-RECONCILIATION.md

This file was generated by the `/doc-truth-up` documentation-reconciliation pass. It treats the
code as read-only ground truth and edits only documentation so it reflects the repo's actual current
state. No source code was modified and no builds or tests were run — all conclusions are derived
by reading source files, manifests, scripts, and git history.

---

## Per-Claim Findings

### 1. What it is

**Status:** `consistent`
**Evidence:** `verified-by-reading-code`

README intro accurately describes AuraFlow (PomGambler) as a browser-based app combining Pomodoro
focus sessions with a virtual prediction market. Key entry point is `index.html:1`. Core modules
confirmed: `js/main.js` (entry), `js/components/PomodoroTimer.js`, `js/components/Dashboard.js`,
`js/models/BettingEvent.js`, `js/services/BettingService.js`. AGENTS.md portfolio context
section (lines 15–56) independently confirms the same description. No edits needed.

---

### 2. Current state

**Status:** `consistent`
**Evidence:** `verified-by-reading-code`

README feature list (lines 11–17) matches code:

| Claimed feature | Confirming file |
|---|---|
| Tiered coin rewards | `js/models/CoinBalance.js`, `js/services/BalanceService.js` |
| Virtual betting market | `js/models/BettingEvent.js`, `js/services/BettingService.js` |
| Manual resolution + payouts | `js/components/History.js`, `js/services/HistoryService.js` |
| Analytics snapshot | `js/services/AnalyticsService.js` |
| Theme switching | `js/components/ThemeSwitcher.js` |
| Interruption detection | `service-worker.js` + `js/services/TimerService.js` |
| Fully offline | `service-worker.js`, `manifest.json` (PWA installable) |

`docs/CLOSEOUT.md` confirms the production-readiness scope is closed. Six regression test suites
exist under `tests/regression/` covering analytics, data export, schema, theme, timer controls, and
variable betting. No edits needed.

---

### 3. Stack

**Status:** `consistent`
**Evidence:** `verified-by-reading-code`

README stack table (lines 36–44) is accurate for local development:

| Row | Claim | Code evidence |
|---|---|---|
| Language | Vanilla JavaScript (ES modules) | All files under `js/` use ES module syntax |
| Storage | IndexedDB + SQLite (local) | `lib/sql-wasm.js`, `lib/sql-wasm.wasm`, `js/core/database.js` |
| UI | HTML + CSS (no framework) | `index.html`, `styles/` — no framework imports |
| Deployment | Static — no build step required | True for local dev; `scripts/ci/build-pages.mjs` handles GitHub Pages packaging (not a compile step) |

**Omission noted (not drifted):** README does not document the PWA layer (`service-worker.js`,
`manifest.json`, `icon-192.png`, `icon-512.png`). This is an incompleteness, not a false claim —
"fully offline" captures the user-visible outcome without naming the mechanism. Left unchanged per
conservative policy.

No edits needed.

---

### 4. How to run

**Status:** `consistent`
**Evidence:** `verified-by-reading-code`

README Quick Start (lines 25–32) documents:
- `open index.html` — valid; no build required
- `python3 -m http.server 8000` — confirmed by `scripts/dev-normal.sh:9` which wraps this exact command

`docs/PRODUCTION_READINESS.md` additionally documents `./scripts/dev-normal.sh` and
`./scripts/dev-lean.sh` as dev modes — both scripts exist and are consistent with the README
instructions. No edits needed.

---

### 5. Known risks

**Status:** `consistent`
**Evidence:** `verified-by-reading-code`

AGENTS.md portfolio context (lines 47–50) lists three risks: stay offline + account-free, keep
betting virtual-only, avoid framework churn. All three map accurately to the repo's architecture
(no backend code, no real-money hooks, no framework dependencies in `js/`). No edits needed.

---

### 6. Next move

**Status:** `consistent`
**Evidence:** `verified-by-reading-code`

`docs/CLOSEOUT.md` recommended backlog (lines 20–23):
1. Add Playwright E2E tests — no Playwright dependency exists; confirmed incomplete
2. Replace prompt-driven custom event creation with inline form modal — `js/services/EventCreationService.js` exists but no modal component found
3. Add import flow for JSON snapshots — `js/services/DataExportService.js` exists (export side) but no import path found

AGENTS.md next move (lines 53–55) aligns with this. No edits needed.

---

### Drifted claim fixed

**File:** `docs/PORTFOLIO-DISPOSITION.md`
**Location:** "Last known reference" table, row 1
**Before:** `origin/main` tip → `f3cbb8d` chore: add initial CHANGELOG
**After:** `origin/main` tip → `6f961db` docs(context): recover portfolio context (#8)

The table was written when the HEAD was `f3cbb8d`. Two subsequent commits landed on `main`:
`4db6f96` (adds this very disposition doc) and `6f961db` (adds AGENTS.md portfolio context).
The "Last substantive commit" row (`f47d67e feat(release): complete production-readiness delivery`)
remains accurate — the two post-tip commits are docs-only.

---

## Contradictions for Manual Review

These are in files outside the allowed edit set. A human should apply the corrections.

### `CHANGELOG.md:9`

**What is wrong:** The entire changelog body reads `[Unreleased]: Initial release`. Multiple
features were shipped before freeze — `docs/CLOSEOUT.md` lists them explicitly: variable betting,
history analytics, theme switching, interruption detection, lean dev ops, GitHub Pages workflow,
production-readiness docs.

**One-line fix:** Replace `- Initial release` with entries derived from `docs/CLOSEOUT.md` under
an appropriate version header (e.g., `## [1.0.0] - 2024-XX-XX` or keep as `[Unreleased]` but
populate actual bullets from the Closeout "Shipped" list).

### `Makefile:3–19`

**What is wrong:** All five targets (`install`, `dev`, `build`, `test`, `lint`) delegate to
`npm install` / `npm run *`, but the repo has no `package.json`. These targets would fail.
The actual verify and test commands are in `.codex/verify.commands` and run `node` directly.

**One-line fix:** Either remove the Makefile targets, replace them with the canonical
`.codex/verify.commands` commands, or add a `package.json` with matching scripts.

---

## Footer

| Field | Value |
|---|---|
| Generated | 2026-05-30 22:35:28 PDT |
| Branch | `docs/truth-up-2026-05-30` |
| HEAD reconciled | `6f961db7986658085e49f18f27b2f714356fe673` |
| Repo | PomGambler (AuraFlow) |
